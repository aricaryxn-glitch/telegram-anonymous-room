services:
  - type: web
    name: telegram-anonymous-room
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: PASSWORD_PEPPER
        sync: false
      - key: ROOM_SIZE
        value: "5"
      - key: MESSAGE_TTL_MS
        value: "3600000"
      - key: STATE_GITHUB_TOKEN
        sync: false
      - key: STATE_GITHUB_REPO
        value: "aricaryxn-glitch/telegram-anonymous-room-state"
      - key: STATE_GITHUB_PATH
        value: "db.json"
